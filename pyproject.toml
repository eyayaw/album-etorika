[project]
name = "yt-realtime-stats"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-api-python-client>=2.0",
    "python-dotenv>=1.0",
    "rich>=13.0",
]
